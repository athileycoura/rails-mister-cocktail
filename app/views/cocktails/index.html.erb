<div class="card-category">
  <h1>Cheers and Beers.</h1>
    <ul>
      <% @cocktails.each do |cocktail| %>
        <li><%= link_to cocktail.name, cocktail_path(cocktail) %></li>
      <% end %>
    </ul>
  <%= link_to 'Add new', new_cocktail_path, class: "btn btn-primary"  %>
</div>
